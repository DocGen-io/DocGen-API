# Project Settings
PROJECT_NAME="DocGen-RAG SaaS"
API_V1_STR="/api/v1"

# Authentication (Change these in production!)
# Generate a secret key using: openssl rand -hex 32
SECRET_KEY="CHANGE_ME_IN_PRODUCTION"
ALGORITHM="HS256"
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=30

# Encryption (Fernet needs 32-byte url-safe base64 key)
# Generate a key using: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY="gTjRQ7I2G_Bv-b81b8k7qf3M3VzX_-AawS0bB3Z9Z0A="

# PostgreSQL
POSTGRES_SERVER="localhost"
POSTGRES_USER="docgen"
POSTGRES_PASSWORD="docgen_password"
POSTGRES_DB="docgen_saas"
POSTGRES_PORT=5432

# Redis
REDIS_URL="redis://localhost:6379/0"

# Weaviate (Worker)
WEAVIATE_URL="http://localhost:8080"
